# Task 4: Context API

Extract the State from the custom hook and make it available via the context api. Access the information from the List and another component.

Create a new context named BookContext.

Create a new Component BooksProvider that holds the state and the context provider.

Integrate the BooksProvider in your application.

Connect the custom hook with the context.

Create a new component named Navigation and show the number of books in the database there
