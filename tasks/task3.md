# Task 3: Custom Hook

Create a custom hook the encapsulates all the logic of the List component. Be careful to not mix different topics in one custom hook.

Cout out everything that is not directly related to the view functionality and put it into one or more custom hooks.

Find out which structures the component needs and provide it as a return value from the custom hook.

Wire the hook and the component together so that the application works again.
